{
  "name": "dishum-server",
  "version": "0.1.0",
  "private": true,
  "description": "Dishum authoritative realtime game server (Colyseus).",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "node --test dist/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@colyseus/schema": "^4.0.7",
    "colyseus": "^0.17.10"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
