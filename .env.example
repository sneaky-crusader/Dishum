# Copy to .env (gitignored) and fill in real values. Never commit .env.

# Personal access token for the Supabase CLI (supabase link / db push).
# Generate at: supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=
